import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    // marginBottom: 10,
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-evenly",
  },
});
